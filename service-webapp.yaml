apiVersion: v1
kind: Service
metadata:
  name: service-webapp
spec:
  type: NodePort
  selector:
    app: pod-webapp
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
